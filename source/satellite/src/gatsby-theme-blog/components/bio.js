import React from 'react'
import { css } from "theme-ui"

export default () => (
  <div
    css={css({
      fontStyle: 'italic',
    })}
  >
    with strategy, design, system and more ... &nbsp;
  </div>
)
